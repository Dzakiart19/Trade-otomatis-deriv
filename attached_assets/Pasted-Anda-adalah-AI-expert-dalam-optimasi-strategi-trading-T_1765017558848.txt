Anda adalah AI expert dalam optimasi strategi trading. Tolong analisis kode trading bot berikut dan berikan rekomendasi perbaikan untuk meningkatkan akurasi sinyal sambil menjaga frekuensi sinyal tetap aktif.

KONTEKS:
Bot ini adalah auto trading bot untuk Binary Options (Deriv) dengan strategi RSI + Multi-indicator confirmation. Saat ini memiliki masalah:
1. Akurasi masih buruk (win rate ~42% dari log)
2. User mengeluh sinyal terlalu hening (bisa tunggu 5 menit tidak ada sinyal)
3. Terlalu banyak consecutive losses (4x loss berturut-turut di log)

KODE SAAT INI:
- File: strategy.py (lihat kode lengkap di context)
- Indikator: RSI, EMA, MACD, Stochastic, ATR, ADX dengan +DI/-DI
- Confluence scoring system (0-100 score)
- MIN_CONFLUENCE_SCORE = 20 (threshold terlalu rendah)
- MIN_CONFIDENCE_THRESHOLD = 0.40
- ADX filter dengan directional conflict detection
- Volatility-based position sizing

MASALAH UTAMA DARI LOG:
1. Banyak signal dengan "ADX directional conflict" tetap dieksekusi dan loss
2. Signal dengan confluence score 47-60 (WEAK-MEDIUM) sering loss
3. EXTREME_HIGH volatility zone signal tetap dieksekusi
4. EMA slope warning diabaikan

PERMINTAAN:
Tolong berikan rekomendasi SPESIFIK untuk mengubah parameter berikut agar:
1. Akurasi naik ke minimal 70-80% win rate
2. Sinyal tetap muncul setiap 1-3 menit (tidak terlalu hening)
3. Reduce consecutive losses dari 4x menjadi maksimal 2x

YANG PERLU DIUBAH (berikan nilai konkret):
1. MIN_CONFLUENCE_SCORE: dari 20 ke berapa? (range: 20-80)
2. MIN_CONFIDENCE_THRESHOLD: dari 0.40 ke berapa? (range: 0.30-0.70)
3. ADX_STRONG_TREND: dari 25 ke berapa? (untuk filter trend strength)
4. RSI entry range: apakah perlu dipersempit dari 20-32 (BUY) dan 68-80 (SELL)?
5. Apakah perlu HARD BLOCK untuk:
   - ADX directional conflict dengan diff > X?
   - EXTREME_HIGH volatility zone?
   - EMA slope conflict?
   - Confluence score < X?

CONSTRAINT:
- Jangan ubah core indicator calculation (RSI, MACD, EMA formula)
- Fokus pada filter logic dan threshold optimization
- Pastikan signal masih muncul setiap 1-3 menit (jangan terlalu ketat)
- Balancing antara akurasi tinggi vs frekuensi signal

OUTPUT YANG DIHARAPKAN:
Berikan rekomendasi dalam format:
```python
# Recommended parameter changes for strategy.py

# 1. Confluence threshold
MIN_CONFLUENCE_SCORE = [nilai_baru]  # from 20, reason: ...

# 2. Confidence threshold  
MIN_CONFIDENCE_THRESHOLD = [nilai_baru]  # from 0.40, reason: ...

# 3. ADX settings
ADX_STRONG_TREND = [nilai_baru]  # from 25, reason: ...

# 4. RSI entry ranges (if needed)
RSI_BUY_ENTRY_MIN = [nilai_baru]  # from 20
RSI_BUY_ENTRY_MAX = [nilai_baru]  # from 32
RSI_SELL_ENTRY_MIN = [nilai_baru]  # from 68
RSI_SELL_ENTRY_MAX = [nilai_baru]  # from 80

# 5. Hard block conditions (add to _check_and_execute_signal)
# ADX directional conflict: BLOCK if diff > [nilai]
# Volatility zone: BLOCK if zone == "EXTREME_HIGH"? (True/False)
# EMA slope conflict: BLOCK? (True/False)
# Confluence too weak: BLOCK if score < [nilai]